// This file previously contained multiple small DTO classes. Those DTOs
// have been split into individual files (one public class per file) to
// avoid visibility and duplicate-definition issues. The original grouped
// file was retained temporarily; it no longer declares any types.

package com.hospital.dto;

// Placeholder file: all DTOs are now in their respective files under
// the same package (e.g. CreateWorkOrderRequest.java, CreateSparePartRequest.java, etc.).
